on: [push]

jobs:
  build_job:
    runs-on: ubuntu-latest
    name: Build the block source code
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: yarn install
      - run: yarn build
